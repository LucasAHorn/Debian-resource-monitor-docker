services:
  nginx:
    image: nginx:stable-alpine
    container_name: dashboard-web
    restart: unless-stopped
    ports:
      - "8080:80"
    volumes:
      - ./html:/usr/share/nginx/html:ro
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf:ro

  api:
    image: node:20-alpine
    container_name: resource-api
    gpus: all
    working_dir: /app
    volumes:
      - ./api:/app
      - /var/run/docker.sock:/var/run/docker.sock
      - /:/host-root:ro
      - /srv/fast:/srv/fast:ro
      - /srv/storage:/srv/storage:ro
      - /usr/lib/nvidia/current:/usr/lib/nvidia/current:ro
    environment:
      NVIDIA_VISIBLE_DEVICES: all
      NVIDIA_DRIVER_CAPABILITIES: utility,compute
    command: sh -c "apk add --no-cache docker-cli gcompat libc6-compat && npm install && node server.js"
    ports:
      - "5000:5000"
    restart: unless-stopped
